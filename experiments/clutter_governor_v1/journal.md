# clutter_governor_v1 — the last cheap knob: slow down where the world is thick

**Opened:** 2026-07-14 · **Owner sanction:**「走『雜物調速』探針」·
**Priors:** the dense frontier's two named roads are both measured
dead — memory (`oracle_memory_v1` 2026-07-12: perfect seen-then-lost
memory, actively cashed 3.4×/flight, buys Δcrash −0.035 < the −0.05
bar; mechanism refinement: *"no kinematically comfortable escape
exists by the time any signal fires; the evade itself creates the
next conflict"*) and conditional recalibration (`dense_recal_v1`
2026-07-14: the clutter wedge refuted; heads UNDER-warn where clutter
peaks = an information limit). The two autopsies compose into one
untried, mechanism-matched knob: **reduce the demand for time**
instead of supplying more information — speed is worth 3× the deaths
(dense crash 0.167 @×1.0 → 0.467 @×2.0). The house oracle-first rule
applies: price the ceiling with privileged clutter before any
deployable version exists.

**The exam-redefinition guard, stated up front:** the frozen
speed-cell scoreboards are NOT touched (history stays comparable).
The governor is a NEW mission contract — "commanded cruise, local
autonomous slowdown" — registered here with its own cells; its
goal-time cost is part of the verdict, reported, never hidden.

## G0 (frozen before any number): the oracle-governor ceiling probe

**Arms, identical seeds, paired:**

- `base` — the deployed champion
  (`output/ppo_wm_policy_edge_hard_xp.zip` via `_policy_factory`),
  untouched.
- `gov` — champion + **OracleGovernor**: per decision, privileged
  local clutter = the dense_recal 8-ray count (same geometry, same
  NEAR=1.0 — one signal definition across campaigns); when clutter ≥
  **2**, interleave the menu's SLOW action on alternating decisions
  (the `OracleFieldSlow` urgency cadence — measured viable; never
  hover, the stop-observe OOD lesson); when open, pass through
  untouched.

**Cells:** dense × speed {1.5, 2.0} (the mechanism's active zone;
historical base crash 0.267 / 0.467), **n = 200 per arm per cell**,
seed0 **162000** (fresh block; paired across arms).

**Bars (frozen):**

- **CEILING EXISTS: pooled paired Δcrash (gov − base) ≤ −0.07** — it
  must beat the memory oracle's corpse (−0.035) decisively to be
  worth a deployable follow-up. On pass, the beams-based deployable
  governor releases as its own registration (the trigger signal is
  already priced: dense_recal's clutter count, separation 0.736 —
  under-qualified as a calibration label, ample as a slowdown
  trigger).
- **Guards:** reached-rate ≥ base − 0.05 per cell (slowing must not
  buy safety with timeouts); goal-time (mean steps) delta reported
  honestly per cell — the price tag is part of the verdict.
- **Vacuous clause:** SLOW-substitution rate ≈ 0 → probe vacuous by
  construction (recorded, redesign — not a negative).
- **REFUTED: Δcrash > −0.07 with non-trivial intervention rate** →
  the governor road closes, and with it the dense arc's cheap-knob
  era: memory oracle dead, calibration knife dead, speed oracle dead
  — *the wall is kinematic-at-any-speed-worth-flying*, and the
  honest remaining road is representation (quarterly-class) or an
  accepted floor. Article #13 material either way.

**Seeds:** 162000–162199 per cell (spent here). Exam seeds untouched.

## Status

- [x] Pre-registration (this file, before any number)
- [ ] G0: base/gov × dense@{1.5, 2.0} → Δcrash verdict
