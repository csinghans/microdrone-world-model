# odoor-v2 — the weakest unit, distilled from its measured ceiling teacher

## Pre-registration (2026-07-07, before any campaign number exists)

The integration gate's path decomposition named this unit first: the
opening-door champion (K3, 0.70-0.74) sits ~20 points under its arena
ceiling (privileged meta oracle: **27/30 = 0.90**, seeds 123000). The
teacher-engineering trail is on the record and closes here: FOUR
scripted live-measurement door pilots landed at 0.27-0.57 (TrackW
0.30/0.57, Waiter 0.50, DoorLive 0.53/0.27 — the live aperture race
mis-centers on fence gaps while the door is narrower than
FENCE_SPACING). The thrice-measured law refines: scripted pilots lose
timing tasks **unless handed the true clock** — the meta oracle IS
the true clock, and teachers are privileged by design.

**Frozen design:** collection = `collect_odoor` (the meta oracle flies
opening_door@1.0, 400 episodes, seeds 125000+ — exam seeds untouched);
student obs = the standard exam-exact stream; BC with val floor
≥ 0.80. Exam = the opening-door skill verbatim (odoor@1.0 bar 0.60,
n=30, seed0 from the skill).

**Frozen signature.** Support = odoor@1.0 ≥ **0.80** at n=30 (clearly
above the champion band, toward the 0.90 ceiling — the integration
path needs ~0.89 per stage). Full = ≥ 0.85. Refuted = ≤ 0.74 (the
champion band): distillation from the ceiling teacher adds nothing
over RL here, and the unit's practical band is 0.70-0.74 — the
integration gate then needs re-pricing (a deployment-threshold
conversation).

## Verdict: BC-alone REFUTED — the heaviest drift tax yet (2026-07-07)

odoor@1.0: **0.367**, crash 63 % (champion band 0.70-0.74; support bar
0.80). Open-loop val 0.925 → closed-loop 0.367 = a **53-point drift
tax**, the repo's worst: the door punishes a 0.2 s timing slip with a
wall, where mgap merely tracks worse. The refuted clause fires by its
letter — but its interpretation ("the unit's band is capped; re-price
the gate") is NOT yet earned: this refutes BC-ALONE, and the measured
two-leg recipe exists (mgap: BC 0.585 → BC+FT 0.880, TYING the
teacher, beating pure RL's 0.82). Waiting is RL-learnable (K3 waits
0.76 s), so the fine-tune leg operates squarely inside its measured
safe zone (own diet, RL-learnable skill).

## odoor-v3 pre-registration (2026-07-07, before any number): the two-leg recipe

One knob: PPO 450k on the BC init, opening_door world only, SB3
defaults verbatim (the surpass-teacher dose). Support = odoor@1.0
≥ 0.80 (n=30); full = ≥ 0.85; refuted = ≤ 0.74 — and only THEN does
"the unit is capped near the champion band" become the earned reading,
with the deployment threshold conversation attached.
